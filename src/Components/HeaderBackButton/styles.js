import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  customButtonHeader: {
    backgroundColor: '#05417C',
    alignItems: 'center',
    justifyContent: 'center',
    width: 35,
    height: 35,
    borderRadius: 30,
  },
});
