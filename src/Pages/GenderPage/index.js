import React from 'react';
import {Text, View} from 'react-native';
import styles from './styles';

const GenderPage = ({title, style}) => {
  return (
    <View>
      <Text>Testing Screen 2</Text>
    </View>
  );
};

export default GenderPage;
